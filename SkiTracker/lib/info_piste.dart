import 'package:flutter/material.dart';

class InfoPiste extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Info Piste"),
      ),
    );
  }
}