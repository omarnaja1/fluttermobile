import 'package:flutter/material.dart';

class SceltaComprensorio extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Scelta del comprensorio"),
      ),
    );
  }
}